services:
  - type: web
    name: glowfmsocialhub
    env: node
    buildCommand: pnpm install --frozen-lockfile && pnpm run build
    startCommand: pnpm run start
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
